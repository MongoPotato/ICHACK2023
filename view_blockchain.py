
import streamlit as st

st.title("Blockchain Interface")

blocks = [] #list of blocks


for block in blocks:
    











# def draw_block(x, y, width, height, color, text):
#     st.markdown(
#         f"""
#         <svg width="{width}" height="{height}">
#             <rect x="{x}" y="{y}" width="{width}" height="{height}" style="fill:{color};stroke-width:3;stroke:rgb(0,0,0)" />
#             <text x="{x + 10}" y="{y + 20}" fill="black">{text}</text>
#         </svg>
#         """,
#         unsafe_allow_html=True,
#     )


# #draw a series of ten rectangles which are all the same size and color from left to right
# for i in range(10):
#     #define the color as red
#     color = "#F63366"
#     #draw the blocks from left to right
#     draw_block(i*0.01, 0, 30, 30, color, "Block " + str(i + 1))


# #define the color as red
# color = "#F63366"
# #draw a block in the bottom left corner 
# draw_block(0, 0, 100, 100, color, "Block 1")









